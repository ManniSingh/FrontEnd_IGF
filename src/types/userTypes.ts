export interface LoginFormValues {
    email: string;
    password: string;
  }

export interface LoginFormValues {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
  }
  